export const routes = {
  home: {
    id: '1',
    name: 'Home',
    path: '/',
    transKey: 'navigationTabHome',
    authRequired: false,
    hideMenu: false,
    cookieRequired: false
  }
};

Object.freeze(routes);
