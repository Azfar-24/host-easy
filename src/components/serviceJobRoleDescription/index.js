import SectionWrap from 'components/sectionWrap';
import React from 'react';

const ServiceJobRoleDescription = ({ data }) => {
  return (
    <>
      <SectionWrap heading={{ heading: data.section_heading }}>
        <div className='section__container'>hmm</div>
      </SectionWrap>
    </>
  );
};

export default ServiceJobRoleDescription;
